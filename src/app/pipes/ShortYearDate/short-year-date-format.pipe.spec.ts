import { ShortYearDateFormatPipe } from './short-year-date-format.pipe';

describe('ShortYearDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortYearDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
