export interface PendingRequest {
        statusName: string,
        requestId: number;
        projectName: string;
        reasonOfTravel: string;
        departureDate: string;
        travelMode: string;
        statusModifiedBy:string;
        returnDate: string
}
